export default function steps(state = [], action) {
    switch (action.type) {
        // case SAVE_STEP:
        //     return [
        //         ...state,
        //         action.step
        //     ];
        default:
            return state;
    }
}
